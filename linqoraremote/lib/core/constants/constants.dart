/// Allows the use of self-signed certificates for SSL connections.
/// Set to `true` to permit self-signed certificates, which can be useful
/// for development or testing environments.
/// Note: This should be set to `false` in production for security reasons.
const bool allowSelfSigned = true;
